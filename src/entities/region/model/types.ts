export interface Region {
    id: number;
    regionName: string;
    regionParts: string[];
    regionCode: string;
    lon: string;
    lat: string;
  }
  
  export interface RegionSearchItem {
    id: number;   
    name: string; 
  }